create an .env file at the root of the project and add the free api:

### `REACT_APP_API=https://jsonplaceholder.typicode.com/`

In the project directory, you can run:

### `npm install`

### `npm start`
